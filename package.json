{
  "name": "my-day-learning-town",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "desktop:dev": "npm run build && electron ."
  },
  "dependencies": {
    "kokoro-js": "^1.2.1",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "electron": "^41.3.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
